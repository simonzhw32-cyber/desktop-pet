# Work Item: PET-007

## 元数据

```yaml
work_item_id: "PET-007"
title: "系统托盘与退出确认"
core_profile: "rich-v1"
status: "COMPLETED"
role: "IMPLEMENTER"
review_status: "APPROVED"
completed_at: "2026-05-10T18:57:00+08:00"
depends_on: ["PET-001", "PET-006"]
```

## Goal

添加系统托盘图标与菜单，提供快捷操作入口和优雅退出流程。

## Scope

**In scope:**
- `src-tauri/src/tray.rs` - 托盘模块
- 系统托盘图标 + 右键菜单
- 菜单项：切换皮肤、显示/隐藏、退出
- 退出确认对话框（Tauri dialog）
- **顺手修复：list_skins 两次读取 skin.json**（Reviewer risk #3）

**Out of scope:**
- 托盘动画/通知气泡
- 自定义托盘图标（使用默认 Tauri 图标）

## Acceptance criteria

- [ ] 系统托盘图标显示在任务栏托盘区
- [ ] 右键菜单包含：显示/隐藏、切换皮肤（子菜单）、退出
- [ ] 点击"退出"弹出确认对话框
- [ ] 点击托盘图标可显示/隐藏桌宠窗口
- [ ] 切换皮肤子菜单列出可用皮肤
- [ ] 选择皮肤后立即切换并保存设置
- [ ] list_skins 仅读取 skin.json 一次
- [ ] `pnpm lint` 通过
- [ ] `pnpm build` 通过

## Commands（闸门）

```bash
pnpm lint
pnpm build
```

## Implementer prompt

你是 Implementer，负责实现 PET-007：系统托盘与退出确认。

**工作目录：** `/root/.openclaw/workspace/desktop-pet/`

### 任务分解

#### 1. Rust 托盘模块

创建 `src-tauri/src/tray.rs`：

```rust
use tauri::{
    image::Image,
    menu::{Menu, MenuItem, Submenu},
    tray::{TrayIcon, TrayIconBuilder},
    Manager, AppHandle, Runtime,
};

pub fn setup_tray<R: Runtime>(app: &AppHandle<R>) -> Result<TrayIcon, Box<dyn std::error::Error>> {
    // 构建菜单
    let show_hide = MenuItem::with_id(app, "show_hide", "Show/Hide", true, None::<&str>)?;
    
    // 切换皮肤子菜单
    let skin_submenu = Submenu::with_id(app, "skin_submenu", "Switch Skin", true)?;
    
    // 动态添加皮肤选项（在运行时更新）
    // TODO: 在 list_skins 后动态添加
    
    let quit = MenuItem::with_id(app, "quit", "Quit", true, None::<&str>)?;
    
    let menu = Menu::with_items(app, &[&show_hide, &skin_submenu, &quit])?;
    
    // 构建托盘
    let tray = TrayIconBuilder::new()
        .icon(Image::from_bytes(include_bytes!("../icons/icon.png")).unwrap())
        .menu(&menu)
        .on_menu_event(|app, event| {
            match event.id.as_ref() {
                "show_hide" => {
                    let window = app.get_webview_window("main").unwrap();
                    if window.is_visible().unwrap() {
                        window.hide().unwrap();
                    } else {
                        window.show().unwrap();
                    }
                }
                "quit" => {
                    // 弹出确认对话框
                    tauri::async_runtime::spawn(async move {
                        use tauri_plugin_dialog::DialogExt;
                        let confirmed = app.dialog()
                            .message("Are you sure you want to quit Desktop Pet?")
                            .title("Quit Confirmation")
                            .ok_button_text("Quit")
                            .cancel_button_text("Cancel")
                            .blocking_show();
                        if confirmed {
                            app.exit(0);
                        }
                    });
                }
                id if id.starts_with("skin_") => {
                    // 切换皮肤
                    let skin_id = id.replace("skin_", "");
                    tauri::async_runtime::spawn(async move {
                        let _ = app.emit("switch_skin", skin_id);
                    });
                }
                _ => {}
            }
        })
        .build(app)?;
    
    Ok(tray)
}
```

#### 2. 添加 dialog 插件

更新 `src-tauri/Cargo.toml`：

```toml
[dependencies]
tauri-plugin-dialog = "2"
```

更新 `src-tauri/src/lib.rs`：

```rust
fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_dialog::init())
        // ... 其他配置
        .setup(|app| {
            crate::tray::setup_tray(app.handle())?;
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

更新 `src-tauri/tauri.conf.json`：

```json
{
  "plugins": {
    "dialog": {}
  }
}
```

#### 3. 注册托盘模块

更新 `src-tauri/src/lib.rs`：

```rust
mod tray;
```

#### 4. 前端监听皮肤切换事件

更新 `src/components/Pet.tsx`：

```tsx
import { listen } from '@tauri-apps/api/event';

// 监听托盘皮肤切换事件
useEffect(() => {
  const unlisten = listen<string>('switch_skin', async (event) => {
    const skinId = event.payload;
    try {
      const skinPath = `assets/skins/${skinId}`;
      const skin = await invoke<SkinConfig>('load_skin', { path: skinPath });
      setSkin(skin);
      setCurrentState('idle');
      frameIndexRef.current = 0;
      
      // 保存设置
      if (settings) {
        await invoke('save_settings_cmd', {
          settings: {
            ...settings,
            current_skin_id: skinId
          }
        });
        setSettings({ ...settings, current_skin_id: skinId });
      }
    } catch (e) {
      console.error('Failed to switch skin:', e);
    }
  });
  
  return () => { unlisten.then(fn => fn()); };
}, [settings]);
```

#### 5. 修复 list_skins 两次读取

更新 `src-tauri/src/skin_manager.rs` 的 `list_skins`：

```rust
pub fn list_skins(skins_base_dir: &Path) -> Vec<SkinInfo> {
    let mut skins = Vec::new();
    
    if let Ok(entries) = fs::read_dir(skins_base_dir) {
        for entry in entries.flatten() {
            if let Ok(file_type) = entry.file_type() {
                if file_type.is_dir() {
                    let skin_json = entry.path().join("skin.json");
                    if skin_json.exists() {
                        let id = entry.file_name().to_string_lossy().to_string();
                        
                        // 只读取一次
                        let (name, valid) = match fs::read_to_string(&skin_json) {
                            Ok(content) => {
                                match serde_json::from_str::<SkinConfig>(&content) {
                                    Ok(skin) => (skin.name, true),
                                    Err(_) => (id.clone(), false),
                                }
                            }
                            Err(_) => (id.clone(), false),
                        };
                        
                        skins.push(SkinInfo {
                            path: format!("assets/skins/{}", id),
                            id,
                            name,
                            valid,
                        });
                    }
                }
            }
        }
    }
    
    skins
}
```

#### 6. 验证

```bash
pnpm lint
pnpm build
```

### 禁止事项
- 不要修改 docs/skin-authoring.md
- 不要硬编码密钥

### 输出要求
汇报：
1. 文件修改列表
2. 闸门命令结果
3. 托盘功能描述

---

*Created by Mochi @ 2026-05-10*