# Work Item: PET-009

## 元数据

```yaml
work_item_id: "PET-009"
title: "CI 完善与文档更新"
core_profile: "rich-v1"
status: "COMPLETED"
role: "IMPLEMENTER"
review_status: "APPROVED"
completed_at: "2026-05-10T19:04:00+08:00"
depends_on: ["PET-001", "PET-008"]
```

## Goal

完善 CI 流水线（添加 cargo clippy），更新项目文档。

## Scope

**In scope:**
- CI 添加 `cargo clippy` 步骤
- 更新 `docs/DEVELOPMENT.md`（测试命令、项目状态）
- 更新 `README.md`（功能列表、截图占位）
- 创建 `CHANGELOG.md` 初始版本

**Out of scope:**
- 性能基准测试
- 覆盖率报告
- 自动发布流水线

## Acceptance criteria

- [ ] CI 包含 `cargo clippy` 步骤
- [ ] DEVELOPMENT.md 更新测试命令和项目结构
- [ ] README.md 包含功能列表和截图占位
- [ ] CHANGELOG.md 记录 v0.1.0 里程碑
- [ ] `pnpm lint` 通过
- [ ] `pnpm build` 通过

## Commands（闸门）

```bash
pnpm lint
pnpm build
```

## Implementer prompt

你是 Implementer，负责实现 PET-009：CI 完善与文档更新。

**工作目录：** `/root/.openclaw/workspace/desktop-pet/`

### 任务分解

#### 1. CI 添加 cargo clippy

在 `.github/workflows/ci.yml` 的 `Run Rust tests` 步骤之前添加：

```yaml
      - name: Run Rust linter (clippy)
        run: |
          cd src-tauri
          cargo clippy --no-default-features -- -D warnings
```

#### 2. 更新 DEVELOPMENT.md

更新 `docs/DEVELOPMENT.md`，添加测试和 lint 命令：

```markdown
## Commands

```bash
# Install dependencies
pnpm install

# Type checking (frontend)
pnpm lint

# Run tests
pnpm test

# Rust tests
cd src-tauri && cargo test

# Rust linter
cd src-tauri && cargo clippy

# Development server
pnpm tauri dev

# Build
pnpm build
pnpm tauri build
```

## Project Status

**Current Version:** 0.1.0

**Implemented Features:**
- ✅ Tauri 2 + React + TypeScript scaffold
- ✅ Skin loading and rendering
- ✅ 11 required states (rich-v1)
- ✅ State machine with frame animation
- ✅ Mouse interaction (hover/click/drag)
- ✅ Skin validator with format/size checks
- ✅ Multi-skin support with settings persistence
- ✅ System tray with context menu
- ✅ Unit tests (13 test cases)
- ✅ CI pipeline (lint/test/build)

**Known Issues:**
- Rust code not tested locally (VM memory limited)
- CSP disabled (enable before release)
- Path traversal check is basic (use canonicalize later)
```

#### 3. 更新 README.md

更新 `README.md`：

```markdown
# Desktop Pet

A cute desktop mascot application built with Tauri 2 and React.

## Features

- 🐾 **11 Animation States** - idle, walk, click, drag, sleep, wake, hover, happy, sad, surprised, wave
- 🎨 **Customizable Skins** - Load your own skins with JSON configuration
- 🖱️ **Interactive** - Click, drag, and hover interactions
- 🌙 **Auto Sleep** - Pet falls asleep after 30 seconds of inactivity
- 🔄 **Multi-Skin Support** - Switch between installed skins via system tray
- 📐 **Skin Validator** - Automatic validation of skin format and dimensions
- 🖼️ **System Tray** - Quick access menu for settings and skin switching

## Screenshots

<!-- Add screenshots here -->
*Coming soon*

## Installation

Download from [Releases](../../releases) (when available).

## Development

```bash
# Clone the repository
git clone https://github.com/yourname/desktop-pet.git
cd desktop-pet

# Install dependencies
pnpm install

# Run in development mode
pnpm tauri dev

# Build for production
pnpm tauri build
```

See [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) for detailed development guide.

## Documentation

- [Agent Playbook](docs/agent-playbook.md) - Multi-agent collaboration workflow
- [Skin Authoring](docs/skin-authoring.md) - How to create custom skins
- [Development Guide](docs/DEVELOPMENT.md) - Build and test instructions

## License

MIT
```

#### 4. 创建 CHANGELOG.md

创建 `CHANGELOG.md`：

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [0.1.0] - 2026-05-10

### Added
- Initial release
- Tauri 2 + React + TypeScript scaffold
- Skin loading system with `rich-v1` profile (11 required states)
- Frame animation with loop/oneshot support
- State machine for automatic state transitions
- Mouse interactions: hover (200ms delay), click, drag
- Click-through window with transparent background
- Skin validator: format check, size validation, PNG signature
- Multi-skin support with settings persistence
- System tray with context menu and skin switching
- Exit confirmation dialog
- Unit tests: 13 test cases covering core functionality
- CI pipeline: lint, test, build, cargo test, cargo clippy

### Known Issues
- CSP disabled in tauri.conf.json (enable before production)
- Path traversal protection is basic (consider canonicalize)
- Rust code requires compilation on local machine (CI will verify)
```

#### 5. 验证

```bash
pnpm lint
pnpm build
```

### 禁止事项
- 不要修改 docs/skin-authoring.md 的契约定义
- 不要硬编码密钥

### 输出要求
汇报：
1. 文件修改列表
2. 文档更新摘要
3. 闸门命令结果

---

*Created by Mochi @ 2026-05-10*