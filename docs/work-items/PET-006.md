# Work Item: PET-006

## 元数据

```yaml
work_item_id: "PET-006"
title: "多皮肤切换与设置持久化"
core_profile: "rich-v1"
status: "COMPLETED"
role: "IMPLEMENTER"
review_status: "APPROVED"
completed_at: "2026-05-10T17:57:00+08:00"
depends_on: ["PET-001", "PET-002", "PET-005"]
```

## Goal

支持加载多个皮肤目录、切换当前皮肤，并将用户偏好（当前皮肤、窗口位置）持久化到本地存储。

## Scope

**In scope:**
- `src-tauri/src/skin_manager.rs` - 皮肤发现与管理
- `list_skins` / `switch_skin` Tauri 命令
- 扫描 `assets/skins/` 下的子目录作为可用皮肤列表
- 设置持久化（使用 Tauri Store 或简单 JSON 文件）
- 保存/恢复：当前皮肤、窗口位置
- **顺手修复：Loading 占位硬编码 512×512**（TECH_DEBT #8）

**Out of scope:**
- 皮肤编辑器 UI
- 在线皮肤商店/下载
- 拖放导入皮肤

## Acceptance criteria

- [ ] `list_skins()` 返回可用皮肤列表（id + name）
- [ ] `switch_skin(id)` 切换皮肤并重载帧
- [ ] `get_settings()` / `save_settings()` 读写持久化设置
- [ ] 应用启动时自动加载上次选择的皮肤
- [ ] 设置文件存储在 Tauri app data 目录
- [ ] Loading 占位使用 skin canvas 尺寸而非硬编码
- [ ] `pnpm lint` 通过
- [ ] `pnpm build` 通过

## Commands（闸门）

```bash
pnpm lint
pnpm build
```

## Implementer prompt

你是 Implementer，负责实现 PET-006：多皮肤切换与设置持久化。

**工作目录：** `/root/.openclaw/workspace/desktop-pet/`

### 任务分解

#### 1. Rust 皮肤管理模块

创建 `src-tauri/src/skin_manager.rs`：

```rust
use std::fs;
use std::path::{Path, PathBuf};
use crate::skin::SkinConfig;

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct SkinInfo {
    pub id: String,
    pub name: String,
    pub path: String,
    pub valid: bool,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct PetSettings {
    pub current_skin_id: String,
    pub window_x: i32,
    pub window_y: i32,
}

impl Default for PetSettings {
    fn default() -> Self {
        Self {
            current_skin_id: "default".into(),
            window_x: 100,
            window_y: 100,
        }
    }
}

pub fn list_skins(skins_base_dir: &Path) -> Vec<SkinInfo> {
    let mut skins = Vec::new();
    
    if let Ok(entries) = fs::read_dir(skins_base_dir) {
        for entry in entries.flatten() {
            if let Ok(file_type) = entry.file_type() {
                if file_type.is_dir() {
                    let skin_json = entry.path().join("skin.json");
                    if skin_json.exists() {
                        // 尝试解析
                        let valid = match fs::read_to_string(&skin_json) {
                            Ok(content) => serde_json::from_str::<SkinConfig>(&content).is_ok(),
                            Err(_) => false,
                        };
                        
                        let id = entry.file_name().to_string_lossy().to_string();
                        let name = match fs::read_to_string(&skin_json) {
                            Ok(content) => serde_json::from_str::<SkinConfig>(&content)
                                .map(|s| s.name.clone())
                                .unwrap_or(id.clone()),
                            Err(_) => id.clone(),
                        };
                        
                        skins.push(SkinInfo {
                            path: format!("assets/skins/{}", id),
                            id,
                            name,
                            valid,
                        });
                    }
                }
            }
        }
    }
    
    skins
}

// 设置文件路径
pub fn settings_path(app_data_dir: &Path) -> PathBuf {
    app_data_dir.join("settings.json")
}

pub fn load_settings(app_data_dir: &Path) -> PetSettings {
    let path = settings_path(app_data_dir);
    match fs::read_to_string(&path) {
        Ok(content) => serde_json::from_str(&content).unwrap_or_default(),
        Err(_) => PetSettings::default(),
    }
}

pub fn save_settings(app_data_dir: &Path, settings: &PetSettings) -> Result<(), String> {
    let path = settings_path(app_data_dir);
    if let Some(parent) = path.parent() {
        fs::create_dir_all(parent).map_err(|e| e.to_string())?;
    }
    let content = serde_json::to_string_pretty(settings).map_err(|e| e.to_string())?;
    fs::write(path, content).map_err(|e| e.to_string())
}
```

#### 2. Tauri 命令

在 `src-tauri/src/commands.rs` 添加：

```rust
use crate::skin_manager::{self, PetSettings};

#[tauri::command]
pub fn list_skins(app: tauri::AppHandle) -> Result<Vec<skin_manager::SkinInfo>, String> {
    let resource_path = app.path().resource_dir().map_err(|e| e.to_string())?;
    let skins_dir = resource_path.join("assets/skins");
    Ok(skin_manager::list_skins(&skins_dir))
}

#[tauri::command]
pub fn get_settings(app: tauri::AppHandle) -> Result<PetSettings, String> {
    let app_data_dir = app.path().app_data_dir().map_err(|e| e.to_string())?;
    Ok(skin_manager::load_settings(&app_data_dir))
}

#[tauri::command]
pub fn save_settings_cmd(app: tauri::AppHandle, settings: PetSettings) -> Result<(), String> {
    let app_data_dir = app.path().app_data_dir().map_err(|e| e.to_string())?;
    skin_manager::save_settings(&app_data_dir, &settings)
}
```

#### 3. 注册模块

更新 `src-tauri/src/lib.rs`：

```rust
mod skin_manager;

.invoke_handler(tauri::generate_handler![
    // ... 现有命令
    crate::commands::list_skins,
    crate::commands::get_settings,
    crate::commands::save_settings_cmd,
])
```

#### 4. 前端启动时加载上次皮肤

更新 `src/components/Pet.tsx`：

```tsx
// 启动时获取设置
useEffect(() => {
    async function init() {
        try {
            // 读取设置获取上次皮肤
            const settings = await invoke<PetSettings>('get_settings');
            const skinPath = `assets/skins/${settings.current_skin_id}`;
            const skin = await invoke<SkinConfig>('load_skin', { path: skinPath });
            setSkin(skin);
        } catch (e) {
            // 回退到默认
            const skin = await invoke<SkinConfig>('load_skin', { 
                path: 'assets/skins/default' 
            });
            setSkin(skin);
        }
    }
    init();
}, []);

// Loading 占位使用 skin canvas 尺寸（修复 TECH_DEBT #8）
if (!frameBase64) {
    return (
        <div style={{ 
            width: skin?.canvas.width || 512, 
            height: skin?.canvas.height || 512 
        }}>
            Loading...
        </div>
    );
}
```

#### 5. 验证

```bash
pnpm lint
pnpm build
```

### 禁止事项
- 不要修改 docs/skin-authoring.md
- 不要硬编码密钥

### 输出要求
汇报：
1. 文件修改列表
2. 闸门命令结果
3. 功能说明

---

*Created by Mochi @ 2026-05-10*