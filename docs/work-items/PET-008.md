# Work Item: PET-008

## 元数据

```yaml
work_item_id: "PET-008"
title: "基础测试"
core_profile: "rich-v1"
status: "COMPLETED"
role: "IMPLEMENTER"
review_status: "APPROVED"
completed_at: "2026-05-10T19:02:00+08:00"
depends_on: ["PET-001", "PET-002", "PET-003", "PET-005"]
```

## Goal

添加基础单元测试和集成测试骨架，确保核心功能可验证。

## Scope

**In scope:**
- Rust 单元测试：皮肤校验器、状态机、皮肤管理
- 前端单元测试：Pet 组件基础逻辑（可选 Vitest）
- 测试命令集成到 `pnpm test`
- CI 测试步骤更新

**Out of scope:**
- E2E 测试（Playwright/Webdriver）
- 性能测试
- 覆盖率报告

## Acceptance criteria

- [ ] `cargo test` 运行 Rust 单元测试
- [ ] 至少 3 个 Rust 测试用例：皮肤校验、状态机切换、皮肤列表
- [ ] `pnpm test` 运行前端测试（或保持占位符）
- [ ] `.github/workflows/ci.yml` 包含 cargo test 步骤
- [ ] 测试通过（或环境受限时代码审查验证）
- [ ] `pnpm lint` 通过
- [ ] `pnpm build` 通过

## Commands（闸门）

```bash
pnpm lint
pnpm build
# cargo test（如有 Rust 环境）
# pnpm test
```

## Implementer prompt

你是 Implementer，负责实现 PET-008：基础测试。

**工作目录：** `/root/.openclaw/workspace/desktop-pet/`

### 任务分解

#### 1. Rust 皮肤校验器测试

在 `src-tauri/src/skin_validator.rs` 添加测试模块：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    #[test]
    fn test_required_states_count() {
        assert_eq!(REQUIRED_STATES.len(), 11);
    }

    #[test]
    fn test_validate_missing_state() {
        let mut skin = SkinConfig {
            schema_version: "1.0".into(),
            core_profile: "rich-v1".into(),
            id: "test".into(),
            name: "Test".into(),
            author: "test".into(),
            canvas: Canvas { width: 512, height: 512 },
            anchor: Anchor { x: 256, y: 512 },
            states: std::collections::HashMap::new(),
        };
        
        // 缺少所有强制键
        let result = validate_skin(PathBuf::from("/tmp").as_path(), &skin);
        assert!(!result.valid);
        assert_eq!(result.errors.len(), 11); // 11 missing states
    }

    #[test]
    fn test_validate_profile_mismatch() {
        let mut skin = SkinConfig {
            schema_version: "1.0".into(),
            core_profile: "wrong-profile".into(),
            id: "test".into(),
            name: "Test".into(),
            author: "test".into(),
            canvas: Canvas { width: 512, height: 512 },
            anchor: Anchor { x: 256, y: 512 },
            states: std::collections::HashMap::new(),
        };
        
        let result = validate_skin(PathBuf::from("/tmp").as_path(), &skin);
        assert!(result.errors.iter().any(|e| e.kind == "profile"));
    }
}
```

#### 2. Rust 状态机测试

在 `src-tauri/src/state_machine.rs` 添加测试模块：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_state_machine_new() {
        let sm = PetStateMachine::new();
        assert_eq!(sm.current_state(), "idle");
    }

    #[test]
    fn test_switch_state() {
        let mut sm = PetStateMachine::new();
        sm.switch_state("happy".into());
        assert_eq!(sm.current_state(), "happy");
        assert_eq!(sm.frame_index(), 0);
    }

    #[test]
    fn test_advance_frame() {
        let mut sm = PetStateMachine::new();
        sm.advance_frame(5);
        assert_eq!(sm.frame_index(), 1);
        
        // 循环到末尾
        sm.advance_frame(5);
        sm.advance_frame(5);
        sm.advance_frame(5);
        assert_eq!(sm.frame_index(), 0); // 循环
    }
}
```

#### 3. Rust 皮肤管理测试

在 `src-tauri/src/skin_manager.rs` 添加测试模块：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_settings_default() {
        let s = PetSettings::default();
        assert_eq!(s.current_skin_id, "default");
        assert_eq!(s.window_x, 100);
        assert_eq!(s.window_y, 100);
    }

    #[test]
    fn test_settings_serialization() {
        let s = PetSettings {
            current_skin_id: "custom".into(),
            window_x: 200,
            window_y: 300,
        };
        let json = serde_json::to_string(&s).unwrap();
        let parsed: PetSettings = serde_json::from_str(&json).unwrap();
        assert_eq!(parsed.current_skin_id, "custom");
    }
}
```

#### 4. 更新 CI

在 `.github/workflows/ci.yml` 的 `ci` job 末尾添加：

```yaml
      - name: Run Rust tests
        run: |
          cd src-tauri
          cargo test --no-default-features
```

#### 5. 验证

```bash
pnpm lint
pnpm build
# cargo test（如有 Rust 环境）
```

### 禁止事项
- 不要修改 docs/skin-authoring.md
- 不要硬编码密钥

### 输出要求
汇报：
1. 文件修改列表
2. 测试用例描述
3. 闸门命令结果

---

*Created by Mochi @ 2026-05-10*